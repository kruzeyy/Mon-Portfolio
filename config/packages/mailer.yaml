framework:
    mailer:
        # API HTTPS Brevo (requis sur Render gratuit : les ports SMTP 25/465/587 sont bloqués).
        # Clé = « Clé API » v3 (Paramètres → SMTP et API), pas obligatoirement la clé SMTP seule.
        dsn: 'brevo+api://%env(urlencode:BREVO_API_KEY)%@default'
